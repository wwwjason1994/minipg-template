const { create, APIs, share, nav, date } = require('../../utils/page/index.js')();

create({
    data: {
    },
    // 不参与渲染的单向变量
    variables: {
    },

    onLoad(options) {
    },

    onShow() {
    },


    onShareAppMessage() {
    },
})
