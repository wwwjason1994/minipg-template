/**
 * 公共接口
 * @author Jenson
 * @createdDate 2018-12-8
 */
import { unifiedRequest as request } from '../index.js';

const api = {
    // formid: '/common/formid/',
};

export default {
    // 保存微信通知埋点
    // sendFormId(options) {
    //     return request('POST', api.formid, options);
    // }
}
